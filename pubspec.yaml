name: clean_architecture_tdd
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Service locator
  get_it: ^2.0.1
  # Bloc for state management
  flutter_bloc: ^0.21.0
  # Valeu equality
  equatable: ^0.4.0
  # Fucntional pragramming thingies
  dartz: ^0.9.2
  # Remote API
  connectivity: ^0.4.3+7
  http: ^0.12.0+4
  # Local cache
  shared_preferences: ^0.5.3+4

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.0 


flutter:

  uses-material-design: true

